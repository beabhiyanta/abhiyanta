---
import ChapterListItem from "./ChapterListItem.astro";
const { chapters } = Astro.props;
---

<div class="flex flex-col gap-8 py-8 sm:col-span-2">
  {chapters.map((chapter: any) => <ChapterListItem chapter={chapter} />)}
</div>
